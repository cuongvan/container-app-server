package helpers;

import common.AppConfig;

public class TestConstants {
    public static final String BASE_URI = "http://localhost";
    public static final int PORT = AppConfig.Inst.HTTP_PORT;
    public static final String BASE_URI_PORT = "http://localhost:" + PORT;
}
